#include <stdio.h>

int main() {
    int n = 6, factorial = 1;

    for (int i = 1; i <= n; i++) {
        factorial *= i;
    }

    printf("There are %d possible ways to arrange %d people in a line.\n", factorial, n);

    return 0;
}
